# Providers

This folder will hold app-level providers for:
- tenant context
- session context
- query cache
- UI state shared across route groups

TODO:
- add provider composition once runtime dependencies are installed
