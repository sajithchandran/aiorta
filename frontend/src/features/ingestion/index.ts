import type { FrontendAreaDescriptor } from "../../feature.types";

export const ingestionFeature: FrontendAreaDescriptor = {
  name: "ingestion",
  category: "feature",
  description: "Upload, parsing status, validation issue, and variable mapping UI.",
  todo: ["Add upload workflow.", "Add mapping and validation screens."]
};
