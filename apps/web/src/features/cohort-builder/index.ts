import type { FrontendAreaDescriptor } from "../../feature.types";

export const cohortBuilderFeature: FrontendAreaDescriptor = {
  name: "cohort-builder",
  category: "feature",
  description: "Interactive cohort criteria builder and snapshot workflow UI.",
  todo: ["Add rule builder shell.", "Add preview and snapshot action surfaces."]
};
