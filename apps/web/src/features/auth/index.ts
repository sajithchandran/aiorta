import type { FrontendAreaDescriptor } from "../../feature.types";

export const authFeature: FrontendAreaDescriptor = {
  name: "auth",
  category: "feature",
  description: "Authentication UI workflows and session-aware client behavior.",
  todo: ["Add login form.", "Add session bootstrap flow."]
};
